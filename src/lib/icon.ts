import type { Icon } from "@tabler/icons-svelte";

/** The shape every Tabler icon has. Re-exported so a component that takes an
 *  icon prop names one type instead of inventing its own. */
export type IconComponent = Icon;
